(executable
  (name tailc)
  (public_name tailc)
  (package tail)
  (modules tailc ast tailparser taillexer)
  (libraries base cmdliner sedlex menhirLib)
  (preprocess (pps sedlex.ppx)))

(menhir
  (modules tailparser)
  (flags "-v" "--dump" "--explain" "--inspection" "--table"))
