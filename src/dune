(executable
  (name tailc)
  (public_name tailc)
  (package tail)
  (modules tailc ast parser lexer)
  (libraries base cmdliner sedlex)
  (preprocess (pps sedlex.ppx)))

(menhir
  (flags -v)
  (modules parser))
